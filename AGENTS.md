# AGENTS.md

このリポジトリでは、すべての回答を日本語で行う。

## 目的

このリポジトリは、『プログラミングコンテスト攻略のためのアルゴリズムとデータ構造』を Rust で学習するためのもの。
コンテスト提出を主目的にせず、アルゴリズムやデータ構造そのものを理解して実装することを優先する。

そのため、問題どおりの標準入力・標準出力に対応しつつ、アルゴリズム本体は型付きの `solve` 関数として分けて単体テストしやすくする。

## ディレクトリ規約

章ごとに `c{章番号}` ディレクトリを作り、その配下に問題ごとの Cargo package を置く。

```text
c2/
  alds1_1_d/
    Cargo.toml
    src/main.rs
c3/
  alds1_1_a/
    Cargo.toml
    src/main.rs
```

- workspace member はルート `Cargo.toml` の `members = ["c*/*"]` で拾う。
- package 名は AOJ の問題 ID を小文字にしたものにする。例: `ALDS1_1_D` -> `alds1_1_d`。
- 各問題は独立した Cargo package として作る。
- 原則として、問題ごとのコードは `src/main.rs` だけで完結させる。

## 問題を用意するときの規約

Codex が新しい問題を用意するときは、学習者がアルゴリズム実装に集中できる状態を作る。

新しい問題には最低限これを用意する。

- 問題ごとの `Cargo.toml`
- `src/main.rs`
- AOJ 問題 ID と問題 URL のコメント
- 型付きの `solve` 関数
- `run(input: &str) -> String`
- 標準入力を読み取って `run` の結果を出力する `main`
- `#[cfg(test)]` のサンプルテスト

`main.rs` の基本形:

```rust
// ALDS1_X_Y: Problem Name
// https://judge.u-aizu.ac.jp/onlinejudge/description.jsp?id=ALDS1_X_Y
use std::io::{self, Read};

fn solve(values: &[i32]) -> i32 {
    let _ = values;

    todo!("ここに解答を実装する")
}

fn run(input: &str) -> String {
    let _ = input;

    todo!("ここに入力のパースと出力の整形を実装する")
}

fn main() {
    let mut input = String::new();
    io::stdin().read_to_string(&mut input).unwrap();
    print!("{}", run(&input));
}

#[cfg(test)]
mod tests {
    use super::run;

    #[test]
    fn sample_1() {
        assert_eq!(run("1 2 3\n"), "0\n");
    }
}
```

ただし、`solve` の引数と戻り値は問題に合わせて自然な型にする。

例:

- 数列を扱う問題: `fn solve(values: &[i32]) -> ...`
- 文字列を扱う問題: `fn solve(s: &str) -> ...`
- 複数の値を扱う問題: `fn solve(n: usize, values: &[i32]) -> ...`
- 行列やグラフを扱う問題: `Vec<Vec<_>>` や辺リストなど、学習しやすい型を使う

`run` は AOJ の入力形式に合わせて文字列をパースし、`solve` の結果を AOJ の出力形式に整形して返す。新しい問題のひな形を作る段階では、`run` も学習者が実装できるように `todo!` のままにする。`solve` には標準入力や出力文字列の都合をできるだけ持ち込まない。

Codex が新しい問題のひな形や学習用の実装例を用意するときは、Rust の命名も学習しやすさを優先する。

- 実際に使う引数や変数には `_values` のような未使用を表す先頭 `_` を付けない。
- アルゴリズムの理解に必要ない補助変数を増やさない。ループ変数など既存の値で表せる状態は二重管理せず、疑似コードとの対応を示したい場合は役割がわかる名前にする。

## 入力文字列の扱い

このリポジトリでは、今後追加する問題は原則として AOJ の形式どおりに標準入力を受け取り、標準出力へ返せる形にする。

- `run(input: &str) -> String` で入力文字列から出力文字列までを扱う。
- `main` は標準入力を読み取り、`run` の戻り値をそのまま出力する。
- `solve` は問題に合わせた自然な型を受け取り、アルゴリズム本体だけを担当する。
- Codex が新しい問題を用意するときは、入力パースや出力整形の完成実装は入れず、`run` に `todo!` を置く。
- 入力パースや出力整形が複雑な問題でも、アルゴリズムの理解を妨げないように `run` と `solve` の責務を分ける。

## 既存実装を触るときの注意

- ユーザーが質問・相談・レビュー依頼をしているだけの場合は、回答内で実装を編集しない。
- 実装を編集するのは、ユーザーが「修正して」「対応して」「実装して」など、明示的に編集を依頼した場合に限る。
- ユーザーが途中まで書いた `solve` の中身を勝手に消さない。
- 型やテストを変更する必要がある場合も、既存の実装意図を保つ。
- 既存コードを置き換える前に、現在のファイル内容を読む。
- 関係ないファイルや生成物は触らない。
- `target/` は管理対象にしない。

## レビュー規約

レビューを依頼された場合は、次の2つの観点を両方含める。

- CS 観点: アルゴリズム、計算量、データ構造、境界条件、問題設定との整合性を見る。
- Rust 観点: 所有権、借用、型、命名、標準ライブラリの使い方、Rust らしい表現を見る。

指摘は重要度の高いものから並べ、必要に応じてファイルと行番号を示す。

## テスト規約

- テストは原則として各問題の `src/main.rs` 内に置く。
- サンプルケースは `sample_1`, `sample_2` のように命名する。
- 追加の確認ケースは `small_case`, `edge_case_*` など、意図がわかる名前にする。
- AOJ サンプルは `run` の文字列入出力一致で検証する。
- アルゴリズムの境界条件や小さいケースは、必要に応じて `solve` の戻り値を直接検証する。

例:

```rust
#[test]
fn sample_1() {
    assert_eq!(run("6\n5 3 1 3 4 3\n"), "3\n");
}
```

## 検証

問題追加やテスト変更後は、少なくともコンパイル確認を行う。

```bash
cargo test --workspace --no-run
```

個別問題を確認する場合:

```bash
cargo test -p alds1_1_d
```
